{
  "name": "warbler",
  "version": "1.0.0",
  "description": "warbling its way into the hearts and minds of the american people",
  "main": "index.js",
  "scripts": {
    "test": "npm test",
    "setup-dev": "rm -rf js/shared && ln -s ../bj-shared js/shared",
    "setup-prod": "rm -rf js/shared && git rm -r --cached js/shared && git submodule add -f git@github.com:maxwerr/bj-shared js/shared && git submodule init && git submodule update"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/maxwerr/warbler.git"
  },
  "author": "maxwerr",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/maxwerr/warbler/issues"
  },
  "homepage": "https://github.com/maxwerr/warbler#readme",
  "dependencies": {
    "express": "^4.13.1",
    "socket.io": "^1.3.6"
  }
}
